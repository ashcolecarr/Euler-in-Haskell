-- Project Euler Problem 2
import Data.Time (getCurrentTime, diffUTCTime)

limit = 4000000

main = do
    start <- getCurrentTime
    
    putStrLn $ "The sum of the even fibonacci terms below " ++ show limit ++ " is " ++ show (multiplesOfThreeOrFiveSum [1..(thousand - 1)]) ++ "."
    stop <- getCurrentTime
    putStrLn $ "Program execution took " ++ show (diffUTCTime stop start) ++ " seconds."

fibonacci :: [Int] -> [Int]
fibonacci x:y:ys = 